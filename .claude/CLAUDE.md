# CLAUDE.md — Project Rules

## Changelog

Before every `git commit` and `git push`, update `CHANGELOG.md`:
- Add an entry under `## [Unreleased]` (or a new dated section if releasing) describing what changed.
- Use the format `[YYYY-MM-DD] - Description`.
- Never commit or push without a corresponding changelog entry for that change.
